import {Button} from "../Button/Button";

export const Win = () => {
    return (
        <div>
        win component will be here
            <div>
                <Button name="Play again" callback={() => {}} />
            </div>
        </div>
    );
};