import {Button} from "../Button/Button";

export const Lose = () => {
    return (
        <div>
            lose component will be here
            <div>
                <Button name="Play again" callback={() => {
                }}/>
            </div>
        </div>
    );
};